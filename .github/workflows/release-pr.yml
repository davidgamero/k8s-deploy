name: release Project

on:
   push:
      branches:
         - main
      paths:
         - CHANGELOG.md
   workflow_dispatch:

jobs:
   release:
      uses: davidgamero/action-release-workflows/.github/workflows/release_js_project.yaml
      with:
         changelogPath: ./CHANGELOG.md

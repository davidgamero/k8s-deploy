name: Tag and create release draft

on:
   push:
      branches:
         - releases/*

jobs:
   tag-and-release:
      uses: davidgamero/javascript-release-workflow/.github/workflows/tag-and-release.yml@david
